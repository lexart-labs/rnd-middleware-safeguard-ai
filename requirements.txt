# Runtime + tooling dependencies (pinned for reproducibility).
#
# NOTE: scikit-learn is pinned to the exact version that produced the committed
# .pkl artifacts in data/models/. A different version may fail to unpickle them.
fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2
google-generativeai==0.8.6
scikit-learn==1.8.0
pandas==3.0.3
joblib==1.5.3
requests==2.34.2

# Test-only
pytest==9.0.3
httpx==0.28.1
